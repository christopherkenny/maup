# maup 0.0.0.9000

* Added `maup_tract_map()` and `maup_tract_plans()` to download county-level redistricting maps and simulation plans from the Harvard Dataverse.
* Added `maup_cache_path()`, `maup_cache_size()`, and `maup_cache_clear()` for managing the local download cache.
